#include "Actor.hpp"

#include <vector>

using namespace std;

void update(unsigned int ticks) {
        /*
        int i;
        vector<>;

        for (i = 0; i < behaviors.size; i++) {
               if () behavior->type() 
        }
        */
}
